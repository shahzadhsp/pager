
import 'package:flutter/material.dart';

class DataVisualizationScreen extends StatelessWidget {
  const DataVisualizationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Visualização de Dados')),
      body: const Center(child: Text('Tela de Visualização de Dados')),
    );
  }
}
